{
  "name": "patch-editor",
  "version": "0.1.0",
  "description": "A preview-first markdown editor: edit through a selection-scoped patch panel instead of a raw source pane.",
  "private": true,
  "main": "main.js",
  "scripts": {
    "start": "electron ."
  },
  "devDependencies": {
    "electron": "^31.0.0"
  }
}
